{
  "name": "@bahama-ai/core",
  "version": "0.1.0-alpha.1",
  "description": "Bahama's plan, execution, state, and secret engine. Published because the unbundled CLI depends on it at runtime; the supported public surface is @bahama-ai/provider-kit, and this package carries no compatibility guarantees of its own.",
  "license": "MIT",
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@bahama-ai/provider-kit": "0.1.0-alpha.1",
    "execa": "^9.6.0",
    "semver": "^7.7.0",
    "yaml": "^2.8.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/semver": "^7.7.0"
  }
}
