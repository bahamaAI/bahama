{
  "name": "@bahama/provider-neon",
  "version": "0.1.0-alpha.4",
  "private": true,
  "description": "Neon provider driver. Serverless Postgres on the user's own Neon account, wrapping the official neon CLI (neonctl): project ensure/adopt, sealed connection-string production, and an in-process SQL migration runner.",
  "files": ["dist", "README.md"],
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@bahama/provider-kit": "0.1.0-alpha.4",
    "pg": "^8.16.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0"
  }
}
