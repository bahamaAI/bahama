{
  "name": "@bahama/provider-test",
  "version": "0.1.0-alpha.3",
  "private": true,
  "description": "Deterministic contract-test provider and reference driver implementation. Simulates a live provider (state file), installation/auth gaps, multiple accounts, and injected failures. Enabled in the CLI only with BAHAMA_ENABLE_TEST=1.",
  "files": ["dist", "README.md"],
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@bahama/provider-kit": "0.1.0-alpha.3",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@bahama/core": "0.1.0-alpha.3"
  }
}
